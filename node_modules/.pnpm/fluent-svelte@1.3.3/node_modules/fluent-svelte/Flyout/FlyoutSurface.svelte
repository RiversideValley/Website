<script >import { fade } from "svelte/transition";
import { circOut } from "svelte/easing";
import { get_current_component } from "svelte/internal";
import { getCSSDuration, createEventForwarder } from "../internal";
/** Specifies a custom class name for the surface. */
let className = "";
export { className as class };
/** Obtains a bound DOM reference to the surface element. */
export let element = null;
const forwardEvents = createEventForwarder(get_current_component());
</script>

<div
	class="flyout {className}"
	use:forwardEvents
	out:fade|local={{ duration: getCSSDuration("--fds-control-faster-duration"), easing: circOut }}
	bind:this={element}
	{...$$restProps}
>
	<slot />
</div>

<style >@-webkit-keyframes flyout-in{0%{opacity:0;transform:var(--fds-flyout-transition-offset,translateY(12px))}to{opacity:1;transform:none}}@keyframes flyout-in{0%{opacity:0;transform:var(--fds-flyout-transition-offset,translateY(12px))}to{opacity:1;transform:none}}.flyout{-webkit-animation:flyout-in var(--fds-control-normal-duration) var(--fds-control-fast-out-slow-in-easing);animation:flyout-in var(--fds-control-normal-duration) var(--fds-control-fast-out-slow-in-easing);background-clip:padding-box;background-color:var(--fds-flyout-fallback-background-default);border:1px solid var(--fds-surface-stroke-flyout);border-radius:var(--fds-overlay-corner-radius);box-shadow:var(--fds-flyout-shadow);box-sizing:border-box;color:var(--fds-text-primary);font-family:var(--fds-font-family-text);font-size:var(--fds-body-font-size);font-weight:400;line-height:20px;min-inline-size:320px;padding:16px;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}</style>
